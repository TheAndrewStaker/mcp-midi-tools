# Architectural Decisions — MCP MIDI Control

Append-only log of non-obvious choices. Each entry explains what was chosen,
why, and what was rejected. Future Claude Code sessions should read this
before proposing architectural changes.

---

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-14 | **Target user = guitarist with a Claude account, not a developer.** Every install, UX, and distribution choice must prioritize a non-technical guitarist over a technical one. | The product's value lands if a working musician can describe a tone and hear it on the AM4. Anything that forces the user to install Node, a C++ toolchain, or edit JSON defeats the premise. |
| 2026-04-14 | **Distribute the MVP as a packaged binary (`.exe`), not as an npm package or source install.** | Follows from the target-user decision. Guitarists should double-click one file. Packaging tool will be chosen when Phase 5 starts (candidates: `@yao-pkg/pkg`, `nexe`, or an Electron shell if a GUI is ever added). Rejected: asking users to install VS Build Tools; depending on upstream `node-midi` prebuilds (release cadence not controlled by us). |
| 2026-04-14 | **Keep `node-midi` as the MIDI library.** Do NOT swap to `@julusian/midi`, `jzz`, or Python `mido` to dodge the Windows native-build friction. | Library swaps to avoid setup pain are technical debt. The native build is a dev-machine problem (solved once with VS Build Tools) and a distribution problem (solved once by the packaged-binary decision above). Neither warrants a runtime dependency change. |
| 2026-04-14 | **ES modules, not CommonJS.** `package.json` sets `"type": "module"`; `tsconfig.json` uses `"module": "NodeNext"`; `__dirname` is derived from `import.meta.url` where needed. | Node 24+ defaults to ESM for loose `.ts` files, the `@modelcontextprotocol/sdk` is ESM-native, and ESM is the forward-compatible choice. A one-session migration now avoids future churn. |
| 2026-04-14 | **Use `tsx` to run TypeScript directly, not `ts-node`.** | `tsx` supports ESM + TS with zero config. `ts-node`'s ESM mode requires tsconfig gymnastics that we would redo every time a new script is added. One-line dependency swap; no runtime implication. |
| 2026-04-14 | **Raw scraped wiki pages (`docs/wiki/`) are gitignored.** The committed ground-truth lives in `docs/BLOCK-PARAMS.md` (and later, more structured extracts). | Wiki pages are ~2 MB of loosely-converted markdown that change when upstream changes; committing them would create noisy diffs and merge conflicts for no product benefit. The scraper (`scripts/scrape-wiki.ts`) is reproducible, so the raw pages can always be regenerated. |
| 2026-04-14 | **Phase 1 ships a "live-tweak" MVP before any preset-binary-format work.** ~~Concretely: get `0x08` firmware handshake, `0x14` read preset number, `0x0F` read preset name, and `0x02` set one parameter on the currently loaded preset. That unlocks a demo-able product without decoding the preset binary.~~ | ~~Reverse-engineering the AM4 preset binary format is the single biggest open risk (undocumented, solo effort, similar projects stall here). The live-tweak path uses only documented-for-Axe-Fx-II function IDs and gives us working AM4 control end-to-end before we commit time to the risky binary work.~~ **SUPERSEDED 2026-04-14 same day** — see next row. |
| 2026-04-14 | **Protocol family is Axe-Fx III, not Axe-Fx II.** Session 02 proved AM4 uses the public Axe-Fx III 3rd-party MIDI spec (0x0A / 0x0B / 0x0C / 0x0D / 0x0E / 0x13 / 0x14) with model byte 0x15. Axe-Fx II commands (0x02 / 0x0F / etc.) return rc=0x05 NACK. Block IDs follow the Axe-Fx III enum (ID_INPUT1=37, ID_OUTPUT1=42, ID_CAB1=62, …) with AM4-specific extensions above ID 200 (observed: ID 206 = almost certainly the Amp block). | Empirical — confirmed against a real AM4 on 2026-04-14. The Axe-Fx II template in earlier SYSEX-MAP drafts was the wrong family. |
| 2026-04-14 | **MVP scope: preset authoring, not live control.** The MVP is "Claude builds a complete preset from a natural-language description and stores it to a slot the user picks." This includes: full block chain, all 4 scenes with per-block channel assignment, and a library of reusable channel-block configurations. It does NOT include: live toggle/bypass, real-time parameter tweaks, or scene-switch-as-feature. | Project owner's explicit direction 2026-04-14. Rationale: toggling/switching is easy to do on the device itself — Claude's value is tone-composition, which no existing AM4 tool offers. This means we cannot avoid the preset-binary reverse-engineering work; it's on the critical path. The earlier "live-tweak de-risking MVP" is retired. |
| 2026-04-14 | **Architecture: puppet the device, don't encode preset binaries.** The AM4 preset `.syx` format is per-export scrambled (confirmed Session 03) and cracking the scramble is weeks of work with uncertain payoff. Instead, the tone agent will (1) send a sequence of parameter-set commands over the AM4's live editor protocol to configure the device's working buffer, (2) issue the already-decoded store command (`0x77/0x78/0x79`) to persist. AM4-Edit itself works this way — it never constructs preset binaries in-memory. | The scramble affects ~22% of bytes per export even with zero content changes, making byte-level RE infeasible without tools like Ghidra on AM4-Edit's 21 MB native binary. Puppeting the device uses the same mechanism AM4-Edit uses, respects AM4 firmware as the canonical encoder, and unblocks the MVP as soon as we RE the `0x01` parameter-set command shape (Session 04's goal). Rejected: statistical multi-export analysis (days of tedious capture), Ghidra disassembly (open-ended timeline), factory-preset-copy-only MVP (user explicitly rejected — "the point is to have described a tone and have it set in the device"). |
| 2026-04-14 | **Preset-write safety protocol.** Destructive write commands (store-to-slot) are NEVER issued outside an explicit write-experiment context. Rules: (1) `scripts/probe.ts` stays read-only forever; (2) write experiments live in a separate `scripts/write-test.ts` with a prominent warning banner; (3) all write testing uses slot **Z04** as the scratch slot — never any other slot — with a backup to disk taken before every write; (4) each write requires explicit user confirmation during the RE phase; (5) in the eventual MCP layer, the read-classify-backup-confirm-write flow is non-bypassable. | CLAUDE.md already says "never write without reading first" and "always confirm before overwriting non-empty non-factory slots." This decision tightens the operational rule set so nothing in the dev process violates those principles. |
| 2026-04-14 | **The wiki scraper parses MediaWiki tables into GFM markdown** instead of stripping them to a placeholder. | The tables on `MIDI_SysEx`, `Presets`, `Effects_list`, etc. contain the actual protocol spec (function IDs, block IDs, parameter IDs). Stripping them destroys the value of the scrape. Trade-off: slightly more scraper complexity, but one-time cost. |
| 2026-04-18 | **Two-phase distribution: private beta first (hosted on `andrewstaker.com`), public launch on GitHub Releases only after Fractal conversation + legal review + code cleanup.** Private beta keeps the repo private and serves a signed `.exe` from an unlisted / token-gated URL on the founder's Vercel site. Public launch migrates to GitHub Releases as the source of truth, flips the repo to public, and starts the P5-007 auto-update polling cycle. | Competitor "Legend" on Fractal forums made claims without artifacts (see memory). We do the opposite: private small-group beta with working artifacts, then public launch after (1) reaching out to Fractal Audio staff with a demo and an interop framing, (2) legal pass on trademark + license + reverse-engineering stance, (3) repo polish. Vercel hobby tier (100 GB/mo) is fine for small-scale beta; becomes a wrong choice at public-launch scale where SmartScreen reputation, auto-update, and ecosystem familiarity push us to GitHub Releases. See `LAUNCH-POST-OUTLINE.md` for the public-launch readiness gate and `FRACTAL-OUTREACH.md` for the Fractal contact plan. |
| 2026-05-03 | **Packager (P5-001): Inno Setup installer bundling `node.exe` + project source + `node_modules/` (including the native `node-midi` `.node` binary). Installs to `%LOCALAPPDATA%\MCP-MIDI-Tools\` (per-user, no admin/UAC), auto-writes `%APPDATA%\Claude\claude_desktop_config.json` to register the server entry, checks the AM4 USB driver presence, and signs the installer with the project's EV code-signing cert. Supersedes the 2026-04-14 row's "single-binary packager (`pkg` / `nexe` / Electron)" framing.** | The 2026-04-14 row assumed a single-binary build was the right shape. The actual constraint is `node-midi` is a native addon, and every single-binary path handles native modules with footgun-grade workarounds: `pkg` extracts `.node` files to temp dirs at runtime (fragile and AV-flagged when signed); Node SEA documents addon support as experimental; `nexe` builds Node from source for hours; Electron-shell adds ~100 MB and frames a headless server as a quittable app. An installer that bundles a Node runtime as ordinary files sidesteps the entire problem — the `.node` file is just a file on disk. Trade-off accepted: ~40-50 MB download vs the ~5-10 MB a real single-binary would have produced. Signing is cleaner too (sign the installer once, no custom Node binary that AV heuristics sometimes treat as suspicious). Rejected: **MSIX** — better SmartScreen reputation but sandboxing makes the `%APPDATA%\Claude\` auto-config write more painful than it's worth for v0.1.0; **NSIS** — close second to Inno Setup, both viable, picked Inno for its Pascal scripting which is more readable than NSIS for the auto-config logic. Auto-update (P5-007) deferred to v0.2.0; v0.1.0 ships with manual installer updates (user re-runs the installer when a new version is announced on the forum thread). |
| 2026-05-03 | **v0.1.0 distribution shape: ZIP + `setup.cmd`, NOT an `.exe` installer.** The bundle is built into `build/staging/` (Node runtime + dist + production node_modules + setup/uninstall wrappers) then packaged as `build/dist/mcp-midi-control-v0.1.0.zip`. Users download, right-click → Properties → Unblock, extract anywhere, double-click `setup.cmd`. The `.exe` installer (Inno Setup, `installer/installer.iss` already drafted) is deferred to v0.2. | Founder strategy call 2026-05-03: forum users (the v0.1.0 audience) are expert enough to handle "extract, double-click setup" and prefer the transparency of seeing the bundle contents. ZIPs sidestep the full Windows SmartScreen "unrecognized publisher" dialog (replaced by a milder Mark-of-the-Web warning that disappears after a single right-click → Unblock). Saves the `.exe` polish for v0.2 when broader-audience reach matters and we have install-friction data from real users. The Inno Setup path stays on disk and works against the same `build/staging/` output, so v0.2 is a config flip plus signing decision, not a redo. Rejected: **pure manual JSON-edit ZIP** (Option 1 in the conversation) — saves ~2 hrs of work but loses the `setup.cmd` polish forum readers actually appreciate; **direct .exe at v0.1.0** — accepts SmartScreen friction for an audience that doesn't need it. |
| 2026-05-03 | **v0.1.0 ships UNSIGNED. No code-signing cert (OV or EV) for the launch.** Users will see Windows SmartScreen "unrecognized publisher" warning and click "More info" then "Run anyway" to complete install. Mitigated by clear SmartScreen instructions in the launch post, GitHub Releases hosting (more trusted than a personal domain), open-source repo so users can read the installer logic, and a documented click-through screenshot/GIF. | Founder budget call 2026-05-03: pre-revenue, unwilling to spend $200-700/yr on a cert before validating product traction. SmartScreen friction is real but recoverable for an open-source launch where users can verify the source. The "for now" is explicit — code signing is a v0.2+ upgrade once the project has traction and revenue. Rejected: **EV cert** (~$300-700/yr, no warnings, recommended on UX grounds but vetoed on cost); **OV cert** (~$200-400/yr, similar SmartScreen friction to unsigned at low download volumes, so the spend doesn't buy proportional UX gain at v0.1.0 scale); **Microsoft Store distribution** (free trust but requires paid dev account, store policy review, and reframes the install flow which we don't want to redo this close to launch). |
| 2026-05-03 | **Product name stays "MCP MIDI Control" through public launch, not renamed to something AM4-specific.** | Multi-device strategy: Hydrasynth Explorer support follows AM4 quickly (per founder direction 2026-05-03, BK-031 in `04-BACKLOG.md`), then Axe-Fx II XL+. A device-generic name carries that scope without rebrand churn at each device addition. Future sessions: do not propose AM4-specific renames during README polish or launch-readiness reviews; the name is intentionally generic. |
| 2026-05-03 | **v0.1.0 ships open source under Apache-2.0 from day one — not closed-source-then-OSS-after-Fractal-blessing.** The repo flips public on launch with a `LICENSE` file at root naming Apache-2.0. The Fractal pre-launch outreach email names the license explicitly so Fractal sees the actual posture before the forum post. Captures stay gitignored (per BK-044 capture policy); decoded artifacts (`SYSEX-MAP.md`, `src/protocol/`, registries) are public. | Founder considered closed-source-at-v0.1.0 with eventual-OSS-after-Fractal-blessing as a "respect to Fractal" gesture. On reflection, that posture is strictly worse for both stated goals: (1) **respecting Fractal** — open source is *evidence the work is clean* (no AM4-Edit code lifted, derived from the published Axe-Fx III MIDI Implementation PDF + own captures); a binary blob is exactly the artifact that *invites* the suspicion of having been built by decompiling AM4-Edit. Apache-2.0 is the strongest legal posture for RE-of-published-protocol-on-user-owned-hardware (DMCA §1201(f) interop, EU SCD interop, prior cases). (2) **helping the community** — BK-044 (community-driven device support workflow) requires OSS; closed-source-first slows that flywheel by however long the closed phase lasts. (3) **operational economics** — closed launch + later OSS flip = two community moments, two README rewrites, two license decisions, two announcement posts; OSS-from-day-1 = one of each. (4) **permission asymmetry** — closed-then-ask-OSS-later puts Fractal in the position of approving an OSS flip rather than just being informed of a clean-interop launch; their path of least resistance is silence either way, and silence is "no objection" either way, but the OSS-from-day-1 path doesn't need the second conversation. The thing genuinely worth asking Fractal about isn't the license — it's project name (use of "AM4"), publishing the SysEx decode notes, and the demo's framing. The license is informational. **Apache-2.0 specifically (not MIT)** because it includes an explicit patent grant, which matters more for protocol-RE work than for typical OSS — contributors who later add device support are protected from patent claims by upstream device manufacturers. Rejected: **closed-source v0.1.0 → OSS v0.X** (the founder's first instinct, examined and discarded above); **MIT** (simpler but lacks patent grant); **source-available license** (BSL/Elastic-style — designed for commercial moats, wrong fit for an interop tool); **Apache-2.0 LICENSE committed but repo private at v0.1.0** (compromise option that gets the legal posture without the public-source exposure — viable but doesn't unlock BK-044 community contributions, so deferred only as a fallback if Fractal asks for source-private during outreach). |
| 2026-04-28 | **MCP tool inputs and outputs use DISPLAY units (or enum strings), never wire/protocol numbers.** This is a project-wide convention that applies to AM4 today and every future instrument. Tools accept the values a musician reads on the device front panel or in a manual ("amp.gain = 4.5", "compressor.ratio = 4", "amp.type = 'Plexi 100W High'"). Each device's encoder layer translates display → wire and hides device-specific scaling: Fractal's `value × scale` packed-float wire format, 14-bit septet encoding, the per-block channel register, bipolar centering, the parked Hydrasynth branch's NRPN auto-scale + patch-buffer `wire/8` storage. **Wire values are an internal protocol detail and never appear in tool surfaces.** | (1) AI agents author iconic tones from manuals/forum posts that are always in display units — display-first means the agent's reasoning maps 1:1 onto the device front panel and the protocol math doesn't burn agent capacity. (2) Musicians read manuals in display units; wire is meaningless to them. (3) Consistency across tools — `set_param`, `apply_preset`, `set_block_type` all share the same input semantics; mixing wire and display would be a permanent footgun. (4) Functionality — pushing scaling into the encoder layer means new instruments declare each param's encoding kind once; tool surfaces stay uniform. Rejected: wire-in API for encoders (forces every iconic-tone author to hand-compute `display × scale` per param, gets bipolar centering wrong half the time). Reference implementation on AM4: `src/fractal/am4/params.ts:resolveEnumValue` (display name → enum int) + `src/server/shared/paramHelpers.ts:resolveValue` (display value → numeric, range-checked). |
| 2026-05-10 | **Hardware-verified contribution workflow.** Every new device-support PR (and every wire-format decode for an existing device) must carry mechanical evidence the contributor tested on real hardware on a named firmware version. Three-layer model: (1) capture file committed to repo, (2) firmware-handshake fingerprint capture, (3) round-trip readback evidence. CI verifies each golden test's expected bytes match the source capture file; firmware-handshake captures must match the device header's declared firmware version. Full design in BK-048 (`docs/_private/04-BACKLOG.md`); pilot device is the Axe-Fx III skeleton. | Goal: forging the evidence should cost more than running the test on a real device. The community-RE survey (`docs/axe-fx-ii-community-re-methodology.md`) found NO existing Fractal RE project requires this — both bspaulding/axe-fx-midi and tysonlt/AxeFxControl accept the contributor's claim implicitly. Our workflow turns the protocol-RE evidence (`.syx` captures) into PR-review artefacts, which raises the contribution bar and gives the project a moat against firmware drift. The Axe-Fx III skeleton is the natural pilot — the founder doesn't own one, so community validation is the only path and the workflow has to actually work. Rejected: trust-based contributions (current OSS norm; works at small scale but degrades with device count and firmware churn); video-only evidence (not CI-auditable, replays poorly during review); manual sign-off without artefacts (doesn't survive contributor handoff). |
| 2026-05-10 | **For non-AM4 Fractal devices where Fractal published a 3rd-party MIDI spec (Axe-Fx II / III / FM3 / FM9) and open-source community libraries exist, the approach is "build informed by, not depend on."** Concretely: lift opcode constants + envelope helpers + response parser shapes from MIT-licensed references (`bspaulding/axe-fx-midi`, MIT, confirmed 2026-05-10) into TypeScript with attribution; use GPL-licensed references (`tysonlt/AxeFxControl`, GPL-3.0, confirmed 2026-05-10) as read-only confirmation oracles. **Never add a runtime dependency on either.** Attribution path when first MIT lift lands: `licenses/axe-fx-midi-MIT.txt` + a `THIRD_PARTY_LICENSES.md` row + per-file header crediting Bradley J. Spaulding + repo URL. | Survey of both projects (2026-05-10) confirmed neither implements the editor-write surface (SET_PARAM function `0x02`, grid-layout WRITE, block add/remove, store-to-location) — they're read-and-navigate only. The editor-write surface is the genuine gap our project fills. The language wall (Rust crate / Arduino C++ vs our TypeScript) plus the display-first translation + param-registry shape + MCP tool descriptions that aren't in either project means a direct dependency saves <30 min of work at the cost of permanent build complexity (WASM/FFI). bspaulding also contains a known-stale constant — `FractalModel::AxeFxII = 0x03` is correct for the original Axe-Fx II but wrong for the XL+ revision (`0x07`), confirmed against the founder's factory bank file; a direct dependency would inherit the bug. Rejected: depend on bspaulding via WASM/FFI (build cost, abandoned project, model-byte bug); ignore both projects (passes up free cross-reference evidence + leaves "we surveyed everything" claim unsupported). |
| 2026-05-10 | **TypeScript remains the language for every Fractal device and every other instrument vendor.** Not switching to Rust despite bspaulding/axe-fx-midi being the most prominent open-source Axe-Fx library. Per-device modules under `src/<vendor>/<device>/` give us the "device-agnostic" property cleanly; the language is the wrong axis to vary on. | Rust is theoretically slightly better at byte-pushing — compile-time byte-shape proofs, embedded targets. Neither matters at this project's scale: 50ms USB MIDI round-trip dominates every operation, we don't target microcontrollers, and our byte-exact golden tests already catch the bugs Rust's type system would prevent at compile time. TypeScript is dramatically better for everything ELSE: same language as the `@modelcontextprotocol/sdk`, the agent-facing tool descriptions (which read like prose), the `zod` validation layer, the test harness, hot iteration via `tsx`. Per-device skeleton work (param-table extraction, MCP tool wiring, display-first translation, MCP tool descriptions) is the actual bulk of any device addition — the wire encoder is a small fraction. Rejected: rewrite in Rust (whole-project cost, no per-feature benefit); embed bspaulding via FFI (build complexity charged against trivial savings); polyglot (some devices in Rust, some in TS — contributor onboarding nightmare, golden-test harness gets harder). |
| 2026-05-10 | **Pivot from "validate our encoder against the wiki spec" to "decode the editor-write surface the wiki doesn't document."** With the v0.1.x Axe-Fx II MVP now hardware-validated on Q8.02 (HW-080/076/077/075/084 all ✅, source-code 🟡→🟢 flip landed), the encoder side is closed. Next-tier features (`axefx2_apply_preset`, `axefx2_set_block_type`, routing, save-to-location) need wire-format decodes the Fractal wiki doesn't cover. Closing that gap via loopMIDI + MIDI-OX captures (HW-082 + HW-083) is the next protocol-RE work; both `axefx2_*` write tools we already ship (set_param, set_block_bypass, switch_scene) prove the wire path works, so the new decodes can be tested as soon as captured. | The corpus survey at `docs/axe-fx-ii-community-re-methodology.md` confirmed no open-source library publicly implements the editor-write surface. The captures we'd produce + decodes we'd publish would be **net-new contribution to the public Fractal RE knowledge base** — the layer that genuinely doesn't exist today in any open-source project (bspaulding, tysonlt, laxu/AxeFx2VirtualPedalboard, FCBInfinity, MFC-Edit). Combined with the hardware-verified contribution workflow above, this positions the project as the first OSS effort with audited end-to-end editor control for the Fractal family. Rejected: ship the MVP and call it done (correct narrowly, but the user wants tone-building from chat which requires the editor-write surface); decode via more `.syx` factory bank analysis (already done; format is scrambled per Session 03 and direct binary RE is weeks of work for one device — the live-capture path is materially cheaper given the loopMIDI+MIDI-OX methodology now in hand). |
| 2026-05-11 | **Display-first contract for `axefx2_set_param`: accept display values for calibrated params, fall back to wire-values for uncalibrated.** Per-param `displayMin`/`displayMax` lives in `KNOWN_PARAMS`; the tool auto-detects mode from value-fits-in-range and offers explicit `interpret: "wire" \| "display"` override. Calibration data is hand-curated from hardware sweeps (HW-079 et al.), inlined directly into the auto-generated `params.ts` with a regen-caveat comment. | The 2026-04-28 project-wide "display-first" decision says tool surfaces use display units. For AM4, that's enforced via hand-curated `cacheParams.ts`. For Axe-Fx II, `params.ts` is auto-generated from wiki + XML, and the wiki documents display ranges for only ~5% of params. HW-079 (2026-05-11) calibrated AMP first-page knobs to a clean wire 0..65534 ↔ display 0..10 linear scale with perfect quarter-scale anchors. Calibrating one block at a time is tractable; HW-088 will test whether the scale generalizes registry-wide. Until then, the tool's auto-detection makes calibrated params display-first while keeping the wire path open for uncalibrated ones — so the agent's reasoning maps 1:1 onto the front panel for params we've measured, without forcing wire math for params we haven't. Rejected: **wait until full registry calibration before shipping** (would block the largest UX upgrade behind ~6 hours of sweeps); **separate `_display` and `_wire` tools** (two tools for one job, doubles description surface); **overlay-file system for calibration data** (cleaner long-term but premature for 5 entries; revisit at ~20 entries). |
| 2026-05-11 | **The 0x74/0x75/0x76 preset-state-dump triple is broadcast-only, NOT bidirectional.** Decoded read-side (Session 60 part 1) with position-as-paramId model confirmed for AMP / Delay / VOL. HW-086 verified the triple is not a working host→device write path: writing it back produces silent partial state changes (select values clamp to max-valid-enum, knob values don't land where written). The editor-write surface for AxeEdit's outgoing state-sync traffic remains undecoded — requires the loopMIDI/ipMIDI bridge approach (HW-082 / HW-083 / HW-085). | The triple appears only in passive device→host captures — first clue it might be broadcast-only. HW-086 tested the bidirectional hypothesis. Test 1 (round-trip captured Volume/Pan 1 triple verbatim): no visible change. Test 2 (modified — position 2 changed 2 → 65534): `volume_taper` clamped to wire=5 (max valid enum index for the 6-option select — consistent with "device parsed and rejected the out-of-range value"); `volume` ended at wire=0 not the wire=65534 we sent. 7 of 9 broadcast positions read back identically — strong confirmation of read-side decode, no confirmation of write semantics. The encoder (`buildStateBroadcastTriple`) stays in the codebase for decode-testing utility; its JSDoc records the HW-086 verdict. Rejected: **strip the encoder** (still useful for decode-side testing and the byte-exact golden); **keep pursuing the triple as a write surface** (evidence is clear; pivot to bridge captures). |
| 2026-05-11 | **Single resolver `findParam(target, name)` for `(block, param-name)` → param descriptor across axefx2 tools.** Previous `paramKey(group, name) + paramByKey(key)` two-step is removed. Resolution is by (groupCode, name) match against `KNOWN_PARAMS`, the same predicate `axefx2_list_params` uses. Regression locked in `scripts/verify-axe-fx-ii-param-lookup.ts` (no-MIDI; under 50ms; wired into `npm test`). | The previous resolver built lookup keys from groupCode (e.g. `"vol.volume"`) and probed `KNOWN_PARAMS` directly. But the registry is keyed by lowercase block slug, not groupCode (e.g. `"volpan.volume"`). For blocks where groupCode ≠ slug — VOL/volpan, CPR/compressor, CHO/chorus, DLY/delay, REV/reverb — every valid param name was silently rejected as "Unknown" even though `axefx2_list_params` reported them as available. The Desktop agent surfaced the bug mid-HW-086. The fix is one function consulting the registry by (groupCode, name) tuple, so `list_params` and `get_param`/`set_param` always see the same set of valid names. Rejected: **rename block slugs to match groupCode** (would require regenerating params.ts and breaking the auto-generator's invariants); **fix the lookup at one of two callsites only** (leaves the bug latent for the other); **make `paramKey` tolerant of both shapes** (papered over the asymmetry instead of removing it). |
| 2026-05-11 | **Probe-and-observe (passive capture + send_sysex experimental probes) is the default workflow for decoding Axe-Fx II editor-write functions, NOT the loopMIDI/ipMIDI bridge.** Captures from `npm run capture-axefx2` against the device's MIDI-In port — a Windows-MIDI-shared-readable port — show the device's 0x64 MULTIPURPOSE_RESPONSE acks for every function byte AxeEdit fires, even though we never see AxeEdit's outgoing payload. Cross-correlating those ACK chains across `samples/captured/session-58-*.syx` (read-only baseline / preset-change / knob-turn / grid-move / block-add) + `session-61-save-attempt.syx` enumerated the candidate write-side function bytes (0x1D save, 0x05 grid write, 0x06 routing) in under an hour, with zero new captures. Wire payloads decode via per-probe grid reads (Session 62/63 0x05 cell-index disambiguation) without ever bridging AxeEdit's outgoing direction. | Session 62 closed save-to-location (function 0x1D) AND grid-write (function 0x05) in one sitting using this approach — both decodes were "novel in the public OSS corpus" per a multi-library survey (bspaulding read-only, laxu read-only, tysonlt-for-III deliberately skips grid writes, JamesDunne format-converter only, Fractal wiki silent on II grid writes). The bridge approach (HW-085 Plan B-2 / ipMIDI Trial / MIDI-OX routing) remains usable as a fallback when probe-and-observe runs out of signal — passive capture reveals the function byte AxeEdit invokes via the device's ACK echo (`0x64 [echoed_fn]`), and the wire format then falls out of (a) cross-referencing OSS libraries that document the same function on related firmware (gen1 wiki documented 0x05's cell-index formula `position = 4*col+row`) and (b) sending experimental payloads from `send_sysex` and observing device state changes via `axefx2_get_grid_layout` reads. The user observation that ipMIDI's 60-min trial restart cycle is tedious is real; probe-and-observe needs only one terminal command + Claude Desktop interaction. Rejected: **bridge-first for every new decode** (cost of setup + AxeEdit port reassignment + trial timer outweighs the benefit when the ACK-pattern technique works); **bridge-only for write surfaces** (would have delayed save-to-location ≥ 1 session and grid-write ≥ 2 sessions); **probe-only without OSS library cross-reference** (bspaulding's byte-exact test case for 0x1D + gen1 wiki's cell-index formula for 0x05 were both load-bearing in the decode — probing alone would have taken longer). |
| 2026-05-11 | **Axe-Fx II setlist tool surface mirrors AM4: `axefx2_apply_preset_at` builds one preset from scratch + saves; `axefx2_apply_setlist` iterates over a list with validation-up-front, on_error stop/continue, dry_run, per-entry ACK timing.** Same input/output shape between devices for cross-device consistency. Internal refactor extracts `buildApplyPresetAtOps(input)` (pure, throws on validation errors) and `runApplyPresetAtOps(conn, ops)` (executes + reports metrics) — both `apply_preset_at` and `apply_setlist` call them. Setlist MVP ships row-2-only (auto-routing works); multi-row routing is HW-104. | The user explicit 2026-05-11: "template-based is not very useful and tedious for users. agents should have near full control" — chose Path B (decode grid writes) over Path A (template-based partial setlist). Mirroring AM4's apply_preset_at + apply_setlist shape means agent + Claude Desktop UX is identical across devices: users say "build me a setlist for my show" and the agent's reasoning works the same way whether the device is an AM4 or Axe-Fx II. Refactor to shared helpers prevents ~330 lines of would-be duplication between apply_preset_at and apply_setlist and keeps a single canonical implementation of the build-from-scratch flow. Row-2-only constraint documented in apply_preset_at's docstring (covers 99% of guitar tone presets — linear chain on row 2 is the dominant pattern); HW-104 follow-up unlocks parallel chains via byte[3] of 0x05 or function 0x06 decode. Rejected: **separate AM4-shaped vs. Axe-Fx-shaped setlist tools** (cross-device-consistency loss for agent UX is worse than the slight feature-mismatch — Axe-Fx has 2-channel blocks vs AM4's 4-channel, but we suppress the difference at the tool boundary by accepting per-block `channel: 'X' \| 'Y'`); **support multi-row layouts at MVP** (would have blocked the entire setlist surface on HW-104 — and multi-row routing is rare for typical guitar tones); **don't refactor before adding apply_setlist** (duplication of 330 lines is permanent maintenance debt; one-time refactor cost is well-amortized). |
| 2026-05-11 | **Verification sources of truth for hardware behavior: front panel > `axefx2_get_param`/`am4_get_param` >>> editor app panel display.** Codified in CLAUDE.md "Verification sources of truth" section. Editor apps (AxeEdit, AM4-Edit) cache UI state and can show stale or placeholder values for freshly-placed blocks. Workaround when the founder needs the editor in sync: close+reopen the editor forces a fresh read. | The HW-086 cycle nearly produced an incorrect verdict because AxeEdit's panel display matched what we expected from "writes succeeded" while the device truth was actually "writes silently rejected." Reading the device's front panel for one parameter immediately disambiguated. Future sessions need to default to device-side verification, not editor-side. The doc rule explicitly says "if front panel or get_param disagrees with the editor, the editor is the one that's wrong." Rejected: **silently fix in code** (would leave future sessions to rediscover the trap); **mention only in HW-086 notes** (too narrow scope — the lesson applies to every test of write behavior). |
| 2026-05-12 | **BK-051 port-resolution: registration order over regex narrowing.** When two device descriptors' `port_match` regexes could both match an ambiguous OS port name (e.g. "Fractal Axe-Fx II Port 1" matches both AM4's `/Fractal/i` catch-all and Axe-Fx II's `/axe-?fx/i`), the dispatcher uses **registration order** to break the tie: whichever descriptor is registered first in `src/server/index.ts` wins. AM4's `/Fractal/i` regex stays intact as a catch-all; Axe-Fx II is registered BEFORE AM4 so its more-specific regex fires first for Axe-Fx-named ports. | The alternative — narrowing AM4's regex to `[/AM4/i]` only — would lose the catch-all behavior for edge-case AM4 port names we haven't seen yet (some USB drivers expose ports as "Fractal Audio MIDI" without the AM4 substring). Registration-order tiebreaking is what the registry already implements (`src/protocol/generic/registry.ts:resolveDevice` walks descriptors in insertion order); using it intentionally here is lower-risk than changing a regex that's worked in production. Future devices follow the same rule: register the more-specific descriptor first; keep the catch-all descriptor's regex broad. Rejected: **narrow each device's regex to be mutually exclusive** (constant maintenance as new devices land, missing edge cases break silently); **explicit priority field on the descriptor** (extra surface area for a rule registration order already encodes). |
| 2026-05-12 | **BK-051 Axe-Fx II descriptor: special-case `connectAxeFxII()` in `connections.ts:ensureConnection` rather than reusing the generic `connect({needles:[label]})` path.** The descriptor's `connection_label: 'axe-fx-ii'` does not substring-match the OS port name `Axe-Fx II Port 1` (dash vs. space), so the generic path would fail silently when picking the port. The dedicated `connectAxeFxII()` helper (already in `src/fractal/axe-fx-ii/midi.ts`) probes for `axe-fx` / `axefx` needles, which DO match the OS port name. The descriptor casts the returned `AxeFxIIConnection` to `MidiConnection` via `as unknown as` — structurally safe because the dispatcher only ever calls `send` + `receiveSysExMatching`, both of which AxeFxIIConnection exposes. | The generic path is shape-correct for devices whose `connection_label` matches their OS port name (AM4's special-case is similar — `'am4'` matches "AM4 USB MIDI"). Axe-Fx II's port name doesn't match its label by simple substring; rather than adding an alias map or renaming the label, the special-case branch is the minimum diff. Future devices follow the same pattern when their `connection_label` doesn't substring-match their OS port name — add a `label === 'X' ? connectX() : ...` branch. Rejected: **rename the label to match the port** (would force descriptor `id: 'axe-fx-ii'` to diverge from its `connection_label`, which is confusing); **add a `port_needles` field to the descriptor** (more surface area than necessary — the dedicated helper already exists); **make `AxeFxIIConnection` extend `MidiConnection`** (cross-cuts the connection-layer interface for one consumer's convenience). |
| 2026-05-14 | **Source code organized as five npm workspace packages under `packages/` — `core`, `am4`, `axe-fx-ii`, `hydrasynth-explorer`, `server-all`. Cross-package imports use scoped package names (`@mcp-midi-control/core/midi/transport.js`); intra-package imports are relative paths (no `@/` alias survives Phase B in `packages/`). Each package builds independently to its own `dist/` with declarations emitted. `tsc-alias` is NOT in the build chain.** | The earlier (pre-Phase B) `src/` monolith hid cross-cutting dependencies — a single file had imports from `@/core`, `@/fractal/shared`, `@/protocol/generic`, `@/server/shared` indiscriminately, which made it impossible to identify what genuinely belonged to "core" vs. a device. Phase A (Session 75) disentangled the boundaries inside `src/`; Phase B (Session 76) moved the now-clean modules into per-package directories. Workspace symlinks (npm's `workspaces: ["packages/*"]`) make cross-package resolution work at runtime without project-references machinery. **Intra-package `@/` was removed entirely (rather than per-package re-aliased)** because tsx + Node ESM + workspaces don't apply `tsconfig` paths consistently across source-vs-dist resolution; relative paths work in every consumer (tsc typecheck, tsx hot-run, built dist, npm-published consumer). **`tsc-alias` was removed** because (a) zero `@/` imports remain in package source, so it has nothing to do, and (b) with `baseUrl: "./src"` set, it was rewriting bare imports like `from 'midi'` (the node-midi npm package) into `from '../midi'` because there's a sibling `./src/midi/` directory — caused `ERR_UNSUPPORTED_DIR_IMPORT` at runtime. Each package's `tsconfig.json` retains `paths: { "@/*": ["./src/*"] }` for IDE intent and future re-aliasing, but no source imports use it. Rejected: **single root tsconfig with paths mapping for all packages** (only works for typecheck, breaks at runtime); **TypeScript project references with `composite: true`** (correct but heavier — incremental build cache, stricter cross-package contracts, more surface area for IDE config drift; can adopt later if multi-package builds become slow); **subpath exports** (using `package.json` `exports` `./*: ./dist/*` already does this for cross-package paths; the open question of also exposing sources for tsx-mode is deferred — current workflow is build-then-run); **publishing packages separately to npm** (deferred — `private: true` everywhere keeps the workspace internal until BK-012 publishes them). The unified surface (17 unified tools + 13 generic-MIDI primitives + per-device descriptors) is unaffected by the layout change; this is purely a code-organization refactor. Hardware verification post-Phase-B: launch-verify 22/22 green on AM4 + Axe-Fx II live. |
| 2026-05-15 | **Fractal devices (AM4, Axe-Fx II, Axe-Fx III) expose NO wire path for reading inactive scenes, inactive channels, or arbitrary stored presets without first navigating to them. The "move-before-read" pattern is the canonical and only approach.** Every tool that needs scene N's state must first `switch_scene(N)` (then optionally switch back); every tool that needs the contents of stored preset P must first `switch_preset(P)`. This is hardcoded into AM4's `describe_device({port:'am4'}).agent_guidance.read_requires_navigation` so the LLM sees the constraint upfront. BK-025 (scene-switch ack payload decode) is **latency optimization**, not a capability unlock — even fully decoded, the move-before-read pattern stays. Session 74 captures confirmed zero indirect-read function IDs observed; the Fractal wiki documents no such function for any device in the family. | Goes hand-in-hand with the dirty-state decision above. Future tool design must assume "to inspect state X, navigate to X first" rather than designing speculative `read_preset(location)` or `read_scene(N)` tools that can never be implemented. This shapes scene-aware tone-building UX: per-scene authoring is N writes interleaved with N–1 scene switches, not N independent writes. Captured here so future sessions don't re-litigate whether decoding ack payloads could unlock indirect reads — they can't. Rejected: **wait until BK-025/026/008 are fully decoded before committing to the pattern** (the impossibility is structural — every Fractal device tested or surveyed lacks the function); **add a synthetic `read_scene` tool that internally switches** (would mask the navigation side-effect from the LLM, which can't reason about whether the user wanted to land on scene N or just inspect it — explicit switch_scene + read is honest about the side-effect). |
| 2026-05-15 | **AM4 dirty-state tracking is a single-source-of-truth polled working-buffer fingerprint on the navigation seam — no parallel code-side classifier.** Session 71 originally landed a hybrid: (a) a code-side send-classifier that marked dirty on outbound write-class messages and clean on switch/save, layered with (b) a lazy fingerprint poll that fired only when the classifier read clean. Session 78 simplified to (b)-only: every `guardActiveBufferOrSave` call dumps the working buffer via HW-045 (~150–200 ms), hashes it, and compares to the cached "expected clean" fingerprint for the active location. Modes: `'discard'` shortcuts the dump; `'warn'` and `'save_active_first'` always poll. Cache baselines refreshed after every clean transition (post-switch, post-save). Implementation in `packages/am4/src/bufferFingerprint.ts` + `tools/safeEdit.ts`. | The classifier's only function was a ~200 ms fast-path when it knew the buffer was dirty (skip the poll, refuse navigation immediately). In conversational use — where navigation happens 5–10× per session and each MCP call is already 500ms–2s of LLM round-trip — that latency savings is invisible. The cost of carrying the classifier was non-trivial: drift category (front-panel and parallel-editor edits the classifier misses), 11 mark-call sites scattered across write tools, conceptual overhead of maintaining two-layer dirty semantics for future contributors. The fingerprint catches every edit source uniformly (our writes, front-panel, AM4-Edit running alongside) in one round-trip on the only seam where the answer actually matters. The `runPostSaveSwitch` gate was also removed in this pass: a successful save is explicit user authorization; navigating to the just-saved target can't surprise them (there's nothing to lose), and running the gate produced a false-positive against the source-location's stale cache. Rejected: **keep the hybrid as the canonical pattern** (drift category + bookkeeping cost > 200 ms savings); **continuously poll like AM4-Edit (~60 Hz)** (mocks AM4-Edit's UI cadence; the MCP server doesn't need to refresh its own UI state at that rate, only check on navigation); **remove the fingerprint and rely on classifier-only** (would re-open the front-panel-edit blind spot HW-107 confirmed as device-design, not a code gap). HW-107 itself is closed as a negative finding — there is no device-sourced push signal to wait for. |
| 2026-05-16 | **Ghidra mining of Fractal editor binaries is the canonical method for protocol RE, not USB captures alone.** Session 82+83 extracted the full per-effect parameter dictionary from AM4-Edit (50 effect families, 1732 paramId/name pairs) and Axe-Edit III (49 families, 2216 pairs) in two evenings of work. Workflow doc: `docs/ghidra-mining-workflow.md`. Each Fractal editor embeds a switch-statement dispatcher (AM4: `FUN_1402e3da0`, III: `FUN_140397a40`) whose case bodies return pointers to per-effect `{int paramId, int padding, const char* nameStr}[]` tables. Reading those bytes gives the complete catalog. Validated at 99% match rate against the existing capture-decoded entries in `params.ts` — confirming the wire mapping `pidLow = block-type pidLow, pidHigh = catalog paramId` (for paramId ≥ 10). Generic params 0-9 are cross-block conventions (0=level, 1=mix, 2=balance, 4=bypass_mode). | Captures decoded ~400 AM4 params over the first six months of this project; Ghidra mining replicated every one of them plus surfaced 376 more UI-referenced params in a few hours. The reproducible workflow (Java scripts + .cmd runners + analysis TS) means future Fractal device support (FM3, FM9, VP4) needs Ghidra setup + run, not weeks of capture sessions. Also opened cross-device knowledge: AM4's AMP block has no separate dispatcher case — it shares the DISTORT family at pidLow=0x003a (DRIVE uses the same family at pidLow=0x0076). This kind of structural finding is invisible from captures alone. Captures remain necessary for: hardware validation of new wire-level claims; per-param metadata (unit/range/enum values) that Ghidra doesn't carry; PATCH and GLOBAL family pidLows (these blocks aren't in `blockTypes.ts` and need a capture of AM4-Edit changing routing/system settings to confirm their wire addresses). Rejected: **continue capture-only RE** (slower by 10×, misses structural patterns like cross-block addressing); **publish only the empirically-verified params** (the Ghidra-derived entries are wire-byte correct with high confidence — 99% match rate against the empirical set proves the technique; the gap is per-param metadata, addressed by adding new entries incrementally with hardware verification of unit/range). |
| 2026-05-16 | **Reassemble fragmented inbound SysEx at the transport seam, not at each device's protocol layer.** node-midi's WinMM backend (RtMidi.cpp:2538-2582) invokes its `message` callback once per MIM_LONGDATA buffer (RT_SYSEX_BUFFER_SIZE = 1024 bytes, RtMidi.cpp:2467) and clears its internal byte vector between callbacks. Any inbound SysEx >1024 bytes therefore arrives as 2–4 separate JS-level events. AM4 preset-dump chunks (3082 bytes each, 4 per dump) and Axe-Fx II preset bodies both trip this. The canonical fix: a stateful reassembler at `packages/core/src/midi/transport.ts:createSysExAssembler` that buffers between F0 and F7 across fragments and emits one whole message per dispatch. Goldens in `scripts/verify-sysex-assembler.ts` (7 cases including the 3082-byte 4-fragment AM4 chunk) protect the reassembly logic without needing a live MIDI port. | This is THE fix that makes the polled working-buffer fingerprint (the 2026-05-15 always-poll dirty-state decision below) actually work. Before it, every navigation poll silently failed `assertDumpMessageShape` ("expected 3082 bytes, got 2048") and the gate fell through to "proceed: true", silently discarding dirty edits on every navigation. The bug had been latent since the bufferFingerprint subsystem shipped in commit 856c641 (2026-05-14) — the launch-verify 17/17 pass at that point was the now-removed code-side classifier doing the work; the polled fingerprint never landed. Rejected: **reintroduce a code-side classifier as a fast-path** (the 2026-05-15 simplification's whole point was to remove that drift category — front-panel and parallel-editor edits would be silently missed by a classifier; the founder explicitly rejected this on 2026-05-16); **patch each device's response parser to accept fragmented input** (smears the fix across every device's protocol layer for a problem that's purely in the transport; downstream code expects one F0…F7 per emit by long-standing convention); **swap to `@julusian/midi` or `jzz`** (library-swap to dodge a 70-line transport-layer fix is the kind of yak-shave the 2026-04-14 node-midi decision explicitly rejected). Symptom never to forget: dirty buffer + un-qualified `switch_preset` returns `acked: true, warning: "...discarded..."` instead of refusing. If a future session sees that again, check `verify-sysex-assembler` is still in `npm test` AND that `createSysExAssembler` is still wired into `connect()`. |
| 2026-05-16 | **AM4 dirty-gate seeds the fingerprint baseline on first connection use, not lazily on first navigation.** New `warmupAM4BaselineIfNeeded(conn)` in `packages/am4/src/tools/safeEdit.ts` is called from every buffer-mutating writer method (`setParam`, `setParams`, `switchPreset`, `savePreset`, `switchScene`, `setBlock`, `setBypass`, `applyPreset`). Reads active location, dumps buffer, caches the hash as the baseline. Idempotent via `WeakSet<MidiConnection>`: first call per connection costs ~150–200 ms (one location read + one buffer dump); subsequent calls return immediately. Closes the "first navigation after server restart silently proceeds" gap — without warm-up, the gate's "no baseline → proceed" graceful-degrade path silently discarded edits when `set_param` was the first AM4 call of a session. Regression test in `scripts/_research/probe-warmup-baseline.ts` exercises set_param → unflagged switch_preset as the FIRST two AM4 calls and asserts the gate refuses (was silently proceeding pre-fix). | The 2026-05-15 always-poll dirty-state row establishes the polled fingerprint as the canonical mechanism, but the cache-lazy seeding had a small silent-fail-open window: server boot → set_param (dirty buffer) → unflagged switch_preset (gate sees empty cache → falls through to proceed → edit lost). Three alternatives considered and rejected. (a) **Drop the gate entirely, push to agent** — loses front-panel-edit catching, burdens agent with state it doesn't have, expands a small bounded code surface into an unbounded prompt-engineering surface. (b) **Flip "no baseline → proceed" to "no baseline → refuse"** — adds friction on every first navigation after restart even when no edit happened, forces explicit `on_active_preset_edited` when none is semantically needed. (c) **Eager warm-up on first tool dispatch** (this choice) — pays a one-shot ~180 ms cost ONCE per server lifetime, then catches every edit category from t=0. Also rejected: **refresh baseline on every event** — would erase the dirty signal we're trying to detect (the baseline must reflect last-known-CLEAN state, not current state; refreshing after set_param would cache the dirty buffer and make it look clean to the next check). The "no-cache simplification" path is not architecturally available for AM4 specifically: HW-107 (Session 74), `docs/fractal-broadcast-vs-poll-research.md`, and `docs/factory-restore-research.md §3(c)` independently confirm AM4 exposes no device-side dirty bit. Axe-Fx II and III are different — they broadcast state and the listener-based `packages/core/src/server-shared/bufferDirty.ts` path is used for them. WeakSet keyed on connection means reconnects (forceReconnect) re-warm naturally without bookkeeping. |
| 2026-05-08 | **For multi-preset CONVERSATIONAL builds, the agent calls `am4_apply_preset_at` in a per-preset loop rather than reaching for `am4_apply_setlist`. Both tools stay in the surface; the steerage is in their tool descriptions.** `am4_apply_setlist` is reserved for cases where the agent has a fully-specified plan in hand (loaded from a config file, copied from prior session notes, etc.). Creative builds where the agent picks amp models, scene structures, and tone targets per song from natural-language direction use the loop pattern: respond with a numbered plan of all N presets, ask the user to confirm with "go", then loop calling `am4_apply_preset_at` once per preset, narrating progress between calls. The user-facing UX is identical (paste once, walk away after "go") — what differs is whether the agent thinks about all N presets simultaneously or in N small focused steps. | Hardware testing 2026-05-08 surfaced two related symptoms of the all-at-once apply_setlist pattern. (1) **Agent second-guessing.** Cramming 15 simultaneous decisions into one tool call meant the agent re-checked earlier choices, re-considered amps it had already picked, and double-checked scene structures across presets — because validation failure of any single entry fails the whole batch, so cross-preset verification became defensive bookkeeping. (2) **Repeated lineage lookups.** ~10 `am4_lookup_lineage` calls during the planning phase, each costing a full inference round (~5-10 sec), driving the pre-"go" wait to ~2 minutes. Per-preset focused decisions distribute the same total thinking across N independent inference rounds where each result is an immediate checkpoint and earlier decisions don't contaminate later ones. The wire activity for the actual saves was always fast (26.1 sec for 15 presets in apply_setlist mode, design-correct); the bottleneck was purely the planning phase, and the planning phase compresses better when distributed than when batched. Co-shipped with `am4_lookup_lineages` (plural batch lookup tool) and an iconic-amps cheat sheet inline in the apply_preset_at description, which together cut the few unavoidable lineage queries to one round-trip. **Not a generic "always loop, never batch" rule** — apply_setlist remains correct when the plan is already fully formed, e.g. a future "load this saved setlist file" workflow. The tool descriptions distinguish the two cases explicitly. Rejected: **remove apply_setlist** (it has a real use case for fully-specified plans even if conversational creative builds prefer the loop); **make apply_setlist internally loop apply_preset_at** (server-side loop has no win — the inference cost is agent-side, not wire-side, and the agent's per-preset focus is what the loop pattern actually buys). |

---

## How to update this file

Add a new row for any decision that a future session could plausibly
second-guess without this context. Examples of decisions that belong here:

- Picking one library over another (and what was rejected).
- Choosing a distribution model, install flow, or licensing approach.
- Module-system or build-tool choices (CJS vs ESM, bundler selection).
- Data-format commitments (IR schema, preset serialization, MCP tool shape).

Examples of decisions that do NOT belong here:

- Renaming a variable.
- Adding a unit test.
- Fixing a bug (belongs in commit messages).
- Internal refactors with no external surface change.

Keep rationales to 1–3 sentences. Link out to longer context (sprint docs,
RFCs, external references) rather than inlining.
